{% extends '@Admin/layout.html.twig' %}

{% block body%}
<a href="{{ path("myshop.admin_editor_user_add") }}">Add new User</a>
    <ul>
        <table border="1">
            <tr>
                <td>Username:</td>
                <td>Email:</td>
            </tr>
    {% for user in userList %}
        <tr>
        <td>{{ user.username }}</td>
        <td>{{ user.email }}</a></td>
        <td><a href="{{ path("myshop.admin_editor_user_edit", {id:user.id}) }}">Edit</a></td>
        <td><a href="{{ path("myshop.admin_editor_user_delete", {id:user.id}) }}">Delete</a><br/></td>
        </tr>
    {% endfor %}
        </table>
    </ul>

{% endblock %}