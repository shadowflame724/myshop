{% extends '@Admin/layout.html.twig' %}

{% block body%}
    <form action="{{ path("myshop.admin_editor_category_add", {id:id}) }}" method="POST">
        {{ form_widget(form) }}
        <input type="submit"/>
    </form>

{% endblock %}